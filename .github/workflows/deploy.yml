name: Deploy SDK

on:
  push:
    branches: [ main, staging ]
  workflow_dispatch:

jobs:
  deploy-staging:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/staging'
    environment: staging
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: package-dist
        path: dist/
        
    - name: Install twine
      run: |
        python -m pip install --upgrade pip
        pip install twine
        
    - name: Deploy to Test PyPI (using your script method)
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.TEST_PYPI_TOKEN }}
      run: |
        echo "🚀 Deploying to Test PyPI using your script method..."
        echo "📦 Files to upload:"
        ls -la dist/
        
        if [ -z "$TWINE_PASSWORD" ]; then
          echo "❌ TEST_PYPI_TOKEN not available!"
          exit 1
        fi
        
        # Use the same command structure as your script but for Test PyPI
        twine upload dist/* -u __token__ -p $TWINE_PASSWORD --repository testpypi --verbose
        
    - name: Verify deployment
      run: |
        echo "✅ Upload completed!"
        PACKAGE_NAME=$(ls dist/*.whl | head -1 | xargs basename | cut -d'-' -f1)
        echo "🌐 Package available at: https://test.pypi.org/project/$PACKAGE_NAME/"

  deploy-production:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: package-dist
        path: dist/
        
    - name: Install twine
      run: |
        python -m pip install --upgrade pip
        pip install twine
        
    - name: Deploy to Production PyPI (exactly like your script)
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
      run: |
        echo "🚀 Deploying to Production PyPI (same as your local script)..."
        twine upload dist/* -u __token__ -p $TWINE_PASSWORD